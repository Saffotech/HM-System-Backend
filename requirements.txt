# HMS Backend - direct dependencies only (run: pip install -r requirements.txt)
# Transitive packages (starlette, cryptography, etc.) are installed automatically.

# API server
fastapi==0.135.3
uvicorn==0.44.0

# Database
SQLAlchemy==2.0.49
psycopg2-binary==2.9.12
alembic==1.18.4

# Auth & security
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Config
python-dotenv==1.2.2

# Validation (EmailStr, request/response schemas)
pydantic==2.13.0
email-validator==2.3.0

# Timezone data (zoneinfo on Windows)
tzdata==2026.1

# Tests
pytest==8.3.4
httpx==0.28.1
